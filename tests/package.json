{
  "name": "immersivebackpacks-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Integration tests that drive a real Vintage Story client through the VSMCP bridge.",
  "scripts": {
    "test": "tsc && node --test \"dist/**/*.test.js\"",
    "test:only": "tsc && node --test \"dist/**/*.test.js\" --test-name-pattern"
  },
  "dependencies": {
    "vsmcp-server": "file:../../VSMCP/mcp-server"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
